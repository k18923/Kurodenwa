# Kurodenwa プロジェクト - やるべきこと

このドキュメントは「このプロジェクトでやるべきことはありますか?」という質問に対する回答です。

## プロジェクト概要

Kurodenwaは、黒電話（601A）をBluetooth HFP（Hands-Free Profile）子機化するESP32ベースのプロジェクトです。PCM5102A（DAC）とPCM1808（ADC）を使用してI2S音声を処理し、KS0835Fでベル鳴動を制御します。

## 完了した作業

### ✅ レガシーコードのクリーンアップ（2026-02-08）
- WM8960コーデックサポートの削除（未使用のため）
  - `main/wm8960.c` および `main/wm8960.h` を削除
  - `main/CMakeLists.txt` からwm8960.cのビルド設定を削除
  - `main/audio_i2s.c` からwm8960のincludeと初期化コードを削除
  - `docs/spec.md` の参照を更新
  - `docs/WM8960.pdf` データシートを削除
  - 実際の実装ではPCM5102A/PCM1808を使用

## やるべきこと（優先順位別）

### P0 - 最優先（Critical）

#### 1. フック操作と通話状態の整合性の修正
**問題**: 受話器の上げ下げ（フック操作）と通話状態が正しく同期していない可能性があります。
- オンフック時に即座に通話を終了する必要がある
- オフフック時に着信応答/発信開始の流れを正しく実装する必要がある

**影響**: 通話の基本動作に関わる重要な問題です。

**該当ファイル**: `main/main.c`, `main/dial_hook.c`, `main/hfp.c`

#### 2. ベル鳴動パターン制御の確認
**現状**: ring_control.cに1秒鳴動→2秒停止のパターンは既に実装済み
- 着信時の鳴動開始・停止条件の確認が必要
- 応答時や切断時の停止タイミングの検証が必要

**該当ファイル**: `main/ring_control.c`, `main/main.c`

### P1 - 高優先度（High Priority）

#### 3. mSBCデコード対応またはCVSD固定の明確化
**問題**: mSBC（Modified Subband Coding、広帯域音声）のサポート状況が不明確です。
- mSBCコーデックのネゴシエーション確認
- CVSDフォールバックの動作確認
- コーデック選択時のログ出力改善

**該当ファイル**: `main/hfp.c`, `main/audio_i2s.c`

**参考**: CLAUDE.mdに「mSBC codec not supported (falls back to mute)」との記載あり

#### 4. 音声経路の整備（音量/ゲイン/ミュート）
**必要な機能**:
- HFPボリュームイベントとI2Sゲインの同期
- 通話中以外のミュート制御（ノイズ防止）
- ユーザー調整可能な音量制御

**該当ファイル**: `main/audio_i2s.c`, `main/hfp.c`

#### 5. 無音時ノイズ対策
**問題**: HFP非アクティブ時にブザー音やノイズが発生する可能性があります。
- バッファ処理の最適化
- ゼロ埋め処理の改善
- 無音時の自動ミュート

**該当ファイル**: `main/audio_i2s.c`

### P2 - 通常優先度（Normal Priority）

#### 6. HFP接続安定性の向上
**問題**: 接続タイムアウトや切断後の自動再接続ロジックが不十分です。
- "Connection Accept Timeout"エラーへの対処
- 自動再接続ロジックの実装・改善
- 接続状態の監視とリトライ機構

**該当ファイル**: `main/hfp.c`

#### 7. ダイヤル入力の活用
**現状**: ダイヤルパルス検出は実装済みですが、番号バッファから発信コマンドへの連携が未整備です。
- 番号入力完了後の自動発信
- 番号バッファのクリア処理
- エラーハンドリング

**該当ファイル**: `main/main.c`, `main/dial_hook.c`, `main/hfp.c`

#### 8. コンソール設定の拡張
**現在のコマンド**: `t`（テストトーン）、`r`（ベル）、`a`（自動応答）、`b`（着信鳴動）、`s`（状態表示）、`?`（ヘルプ）

**追加候補**:
- ログレベルの変更
- HFP自動接続の有効/無効
- 音量調整
- 各種タイムアウト設定

**該当ファイル**: `main/main.c`

#### 9. KS0835F省電力制御
**機能**: KS0835FのPD（パワーダウン）ピン制御
- 待機時の省電力モード
- ベル鳴動時の電力管理
- 適切な起動/停止シーケンス

**該当ファイル**: `main/ring_control.c`

**注意**: PD制御時は直接HIGHに接続しないこと（README.mdの配線メモ参照）

### P3 - 低優先度（Low Priority）

#### 10. ドキュメント整合性の確認
以下のドキュメントが最新の実装と一致しているか確認:
- `README.md`
- `CLAUDE.md`
- `docs/spec.md`
- `IMPLEMENTATION_STATUS.md`

特に：
- ハードウェア構成の正確性
- GPIO割り当ての確認
- ビルド手順の検証

## テクニカルメモ

### 現在の構成
- **ボード**: ESP32-DevKitC WROOM-32
- **フレームワーク**: ESP-IDF v5.5.2
- **オーディオ**: PCM5102A (DAC, 48kHz/32bit), PCM1808 (ADC, 8kHz→48kHz変換)
- **電話インターフェース**: KS0835F (SLIC)
- **言語**: C

### 状態マシン
```
IDLE (待受) ←→ RINGING (着信)
  ↓
OFFHOOK_IDLE (受話器を上げた) → DIALING (ダイヤル中)
  ↓
OUTBOUND_RING (呼び出し中) → TALKING (通話中)
```

### 主要な制約事項
- mSBCコーデックは未サポート（CVSDにフォールバック）
- GPIO0はMCLK出力に使用（ブートストラップピンのため注意が必要）
- I2Sは48kHz/32bit、HFPは8kHz/16bit（リサンプリングが必要）

## 次のステップ

1. **P0タスクの対応**（最重要）
   - フック操作と通話状態の整合性を修正
   - ベル鳴動パターンの動作確認

2. **P1タスクの対応**
   - 音声品質の改善（コーデック、音量、ノイズ）

3. **P2タスクの対応**
   - 接続安定性と使い勝手の向上

4. **テストとデバッグ**
   - 実機でのエンドツーエンドテスト
   - 各状態遷移の動作確認
   - エッジケースの処理確認

## 参考リンク

- `TODO.md`: 元々のTODOリスト
- `IMPLEMENTATION_STATUS.md`: 実装状況の詳細
- `docs/spec.md`: 技術仕様書
- `CLAUDE.md`: Claude Code用のガイダンス
