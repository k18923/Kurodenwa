#include "ring_control.h"

#include "esp_log.h"

static const char *TAG = "ring_ctl";

esp_err_t ring_control_init(void) {
    ESP_LOGI(TAG, "Ring control placeholder (KS0835F)");
    return ESP_OK;
}
